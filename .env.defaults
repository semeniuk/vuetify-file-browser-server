PORT=8081

# if empty then require('os').homedir() is used
FILEBROWSER_ROOT_PATH=

# if empty then require('os').tmpdir() is used
FILEBROWSER_UPLOAD_PATH=

# AWS
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=
AWS_S3_BUCKET=